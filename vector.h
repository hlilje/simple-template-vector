template <class T>
class Vector
{
    private:
    public:
        void push_back(T);

        void insert(std::size_t, T);

        void clear();

        void erase(std::size_t);

        std::size_t size();

        std::size_t capacity();

        begin();

        TODO end();

        TODO find(const T&);
};
